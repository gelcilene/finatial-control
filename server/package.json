{
  "name": "finatial-control-server",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node src/server.js",
    "test": "NODE_ENV=test node --test",
    "deps": "HTTP_PROXY= HTTPS_PROXY= http_proxy= https_proxy= npm_config_proxy= npm_config_http_proxy= npm_config_https_proxy= npm install --no-progress --registry=https://registry.npmjs.org"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5",
    "supertest": "^7.1.1"
  }
}
